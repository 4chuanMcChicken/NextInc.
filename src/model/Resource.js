// resourceStore.js

// Create and export resourceMap
const resourceMap = new Map(); // Map to store resources

// Create and export currentId (initial value for auto-incrementing IDs)
const currentId = 1;

module.exports = {
    resourceMap,
    currentId,
};
